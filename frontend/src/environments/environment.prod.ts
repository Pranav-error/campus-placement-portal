// Production build target — swapped in via angular.json fileReplacements
// for the "production" configuration. Updated once the backend is deployed.
export const environment = {
  production: true,
  apiBaseUrl: 'https://backend-production-0b5f.up.railway.app/api',
};
